using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CodingPractice : MonoBehaviour
{

    //write a class
    class Practice
    {
        //declare primitive data members
        //declare a method that returns something

    }

    //write another class
    
    //declare a data member to hold an object of type "Practice"
    //declare other primitive types
    //declare a method that uses something in the "Practice" object and returns something

    // Start is called before the first frame update
    void Start()
    {
        //create an object object of the type of your second class
        //print whatever the method in the second class returns. 
        print("something");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
